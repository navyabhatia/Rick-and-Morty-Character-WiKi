/*import React from 'react' ;


const SavedCharacters = ({results}) => {
    let save;
    if(results){
        save = results.map((x) => {
            let { id, name, image, location, status } = x;
           
    return (
        <div>
            saved characters {save};
        </div>
        );
      });
    }
};

export default SavedCharacters;
*/
import React from 'react'

const SavedCharacters = () => {
    return (
        <div>
            saved char here;
        </div>
    )
}

export default SavedCharacters
